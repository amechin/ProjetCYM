{% extends 'base.html.twig' %}

{% block title %}Correspondance{% endblock %}

{% block body %}
    <h1>Correspondance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ correspondance.id }}</td>
            </tr>
            <tr>
                <th>ExerciceId</th>
                <td>{{ correspondance.exerciceId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('correspondance_index') }}">back to list</a>

    <a href="{{ path('correspondance_edit', {'id': correspondance.id}) }}">edit</a>

    {{ include('correspondance/_delete_form.html.twig') }}
{% endblock %}
