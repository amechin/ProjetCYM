<?php


namespace App\Service;

use Symfony\Component\HttpFoundation\Session\SessionInterface;

class SauvExercices
{
/*    public static function SauvExos(SessionInterface $session, array $tab)
    {
        $session->set('idSession', $session->getId());
        $session->set('tab', $tab);

        return $session;
    }*/
}
